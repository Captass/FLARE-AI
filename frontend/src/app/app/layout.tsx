/**
 * Layout for the authenticated app route /app.
 * No public SEO metadata — this is the private application shell.
 */
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
